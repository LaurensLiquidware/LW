# Stage 2 (Python) only - Stage 1 is dependency-free PowerShell by design.
requests==2.33.1
jsonschema==4.26.0
packageurl-python==0.17.6

# Test-only, but pinned here too rather than a separate file for this PoC's size.
pytest==9.1.1
